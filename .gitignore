
# Android / Gradle
android/.gradle/
android/local.properties
android/**/build/
android/.idea/
android/*.iml
android/captures/
android/.cxx/

# Kotlin / Backend
backend/.gradle/
backend/build/
backend/**/build/
backend/.idea/
backend/*.iml

# Env / secrets
*.env
.env.local
**/secrets.properties
**/*serviceAccountKey*.json
# NOTE: android/app/google-services.json is intentionally tracked as a placeholder
# (fake project/keys) so the project builds out of the box. Replace its contents with
# your real Firebase config locally, but do not commit the replaced (real) file.

# OS
.DS_Store
Thumbs.db
